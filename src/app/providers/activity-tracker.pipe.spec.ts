import { ActivityTrackerPipe } from './activity-tracker.pipe';

describe('ActivityTrackerPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityTrackerPipe();
    expect(pipe).toBeTruthy();
  });
});
